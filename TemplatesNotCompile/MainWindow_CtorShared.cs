namespace SunamoWpf.TemplatesNotCompile;

public partial class MainWindow_Ctor
{
    public enum Mode
    {
        // Empty First mode in every app
        Empty,

        // Then Modes of app

        // then shared UC for every app
        LogUC,
        Settings,
        CheckBoxListMode
    }
}